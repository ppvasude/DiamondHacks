- job:
    name: iTrust2
    builders:
      - shell: 'ansible-playbook /jenkins-srv-files/setup-iTrust-repo.yml -i /jenkins-srv-files/inventory'
      - shell: 'sudo mvn -f pom-data.xml process-test-classes'
      - shell: 'sudo mvn clean test verify checkstyle:checkstyle -Djetty.port=9999'