server {
        listen 80 default_server;
        listen [::]:80 default_server;
        root /vars/libs/jenkins/workspace/checkbox_build/public_html;
        index index.js;
        location / {
                try_files $uri $uri/ =404;
        }
}